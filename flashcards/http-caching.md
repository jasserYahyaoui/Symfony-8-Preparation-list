# Flashcards Symfony 8.0 — HTTP Caching

Format examinateur (niveau certification):
- **Q:** question courte et piégeuse.
- **R:** réponse opérationnelle (définition + décision pratique).
- **Point examinateur:** ce qui tombe souvent en confusion.

## HttpCache component

**Q:** Explique "HttpCache component" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Sur le thème **HTTP Caching**, maîtriser ce sous-sujet en définition, configuration et cas d’usage concret.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.

## Cache types (browser, proxies, and reverse-proxies)

**Q:** Explique "Cache types (browser, proxies, and reverse-proxies)" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Sur le thème **HTTP Caching**, maîtriser ce sous-sujet en définition, configuration et cas d’usage concret.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.

## Expiration (Expires, Cache-Control)

**Q:** Explique "Expiration (Expires, Cache-Control)" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Sur le thème **HTTP Caching**, maîtriser ce sous-sujet en définition, configuration et cas d’usage concret.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.

## Validation (ETag, Last-Modified)

**Q:** Explique "Validation (ETag, Last-Modified)" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Appliquer contraintes/groupes/séquences selon scénario métier.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.

## Client side caching

**Q:** Explique "Client side caching" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Distinguer expiration (freshness) et validation (ETag/Last-Modified), puis choisir la bonne stratégie.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.

## Server side caching (reverse proxy / gateway)

**Q:** Explique "Server side caching (reverse proxy / gateway)" en Symfony 8.0 et donne une règle de décision en contexte d’examen.

**R:** Distinguer expiration (freshness) et validation (ETag/Last-Modified), puis choisir la bonne stratégie.
Point examinateur: donner le mécanisme exact attendu par le blueprint, sans approximation.
